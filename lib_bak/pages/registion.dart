import 'package:flutter/material.dart';

class RegistionPage extends StatelessWidget {
  const RegistionPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('registion page'),
      ),
      body: Container(
        child: Text('body'),
      ),
    );
  }
}
